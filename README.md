# Shipping-Container-Inventory-System
An object-oriented system for managing shipping containers. There are two types: manual containers (with a text manifest) and RFID containers (which track individual items and their counts). Demonstrates inheritance, polymorphism, and dynamic memory allocation. 
